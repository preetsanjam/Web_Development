// Run the code in the browser console

// Navigator refers to browser 

console.log(navigator);

// Every browser and every version of a browser has a unique userAgent string
console.log(navigator.userAgent);

