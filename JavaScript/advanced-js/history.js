// Run the code in the browser console

console.log(history);

// Three important methods of history object
history.back();
history.forward();
history.go(-2); // Going 2 pages backwards

history.pushState()